# Optional — only needed when deploying the frontend separately from the backend.
# Leave blank when running Vite dev server (proxy handles /api automatically).
VITE_API_URL=

# Must match ADMIN_TOKEN set on the backend server.
# Leave blank for local dev when ADMIN_TOKEN is also not set on the server.
VITE_API_TOKEN=your-secret-token-here
